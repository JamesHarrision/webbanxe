import type { ThemeConfig } from 'antd';

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: '#1890ff', // Standard Blue for now, will update to VinFast specific later if needed
  },
};

export default theme;
